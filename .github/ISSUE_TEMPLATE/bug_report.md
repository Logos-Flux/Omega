---
name: Bug report
about: Report something that isn't working as documented
labels: bug
---

## Description

A clear, concise description of the bug.

## Affected component

Which app or package: `chat-api` / `chat-frontend` / `controller` / `pi-harness` / `rag-api/api` / `rag-api/ingest` / `packages/shell` / other.

## Steps to reproduce

1.
2.
3.

## Expected behavior

What should have happened.

## Actual behavior

What happened instead. Include error messages and stack traces (in code blocks) if available.

## Environment

- Omega version or commit SHA:
- Deploy mode: `docker-compose` / `proxmox-lxc` / `fly` / `local dev` / other:
- OS / arch:
- Bun version (`bun --version`):
- Browser (for `chat-frontend` issues):

## Additional context

Logs, screenshots, related issues — anything else that helps reproduce.
